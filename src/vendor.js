/** Vendor library references
 * used by webpack for the production build only
 * Anything not referenced here will be bundled in the application JavaScript bundle.
 */

/* eslint-disable no-unused-vars */

import fetch from 'whatwg-fetch';
